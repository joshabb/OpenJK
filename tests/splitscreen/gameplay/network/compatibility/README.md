# GP3-02 compatibility discovery

Runs frozen-native stock listen-server cases with `sv_pure` off and on for
2/3/4 local clients. It narrowly certifies observed checksum emission,
client-slot/lifecycle assertions, restart recovery, and stock map transition.

Downloads, third-party mods, QVM compatibility, corrupt/unavailable content,
disk exhaustion, cancellation, and external product/protocol matrices require
controlled content/endpoints not present in this workspace and remain
`BLOCKED_UNSUPPORTED`.
