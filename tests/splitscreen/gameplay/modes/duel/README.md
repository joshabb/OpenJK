# GP2-02 Duel discovery

The runner refuses any executable except the frozen Phase 2 SHA, launches each
Duel/Power Duel capacity through the Phase 0 harness, validates every manifest,
and emits an immutable matrix. Missing authoritative queue/role/camera
instrumentation is marked unsupported rather than inferred.
